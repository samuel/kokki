check process supervisord with pidfile {{ env.config.supervisor.pidfile }}
	start program = "{{ env.config.supervisor.binary_path }}/supervisord -c {{ env.config.supervisor.config_path }}/supervisord.conf"
	stop  program = "/usr/bin/killall supervisord"
	if 5 restarts within 5 cycles then timeout
