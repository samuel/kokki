#!/usr/bin/env python

from __future__ import with_statement

import logging
import sys
from kokki import Kokki

if __name__ == "__main__":
    config = sys.argv[1]
    roles = sys.argv[2:]

    logging.basicConfig(level=logging.INFO)

    kokki = Kokki(config)
    kokki.run_roles(roles)
